"""Real-hardware probes and the prospector that runs the CCS loop on the host CPU.

This is where CCS eats its own dog food: the machine running the platform is
treated as the deployed device, and we search its latent capability space.
"""
